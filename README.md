# Specific Resume post images

Public on purpose: Buffer can only attach an image from a public URL. This repo holds
**only** images for posts that are being scheduled, added right before scheduling, so nothing
unposted appears here early. The source and the design system live in a private repo.
